---
deployment:
  tasks:
    - export DEPLOYPATH=/home/zeme1231/public_html/tchooz
    - /bin/cp * $DEPLOYPATH
    - cd $DEPLOYPATH
    - /usr/local/bin/composer install --no-dev
    - /usr/local/bin/npm install
    - /usr/local/bin/npm run build
