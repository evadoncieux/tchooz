<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Welcome!{% endblock %}</title>
    <link rel="icon" href="{{ asset('build/images/favicon.png') }}">
    <link rel="icon" type="image/x-icon" href="{{ asset('img/favicon.png') }}">
    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
    {% endblock %}

    {% block javascripts %}
        {{ encore_entry_script_tags('app') }}
        {{ encore_entry_script_tags('theme-switcher') }}
        {% block importmap %}{{ importmap('app') }}{% endblock %}
    {% endblock %}
</head>
<body>
<header>
    <div class="nav">
        <p class="title-nav"><a href="{{ path('app_home') }}">tchooz</a></p>

{#        <input type="checkbox" id="hamburger-input" class="burger-shower"/>#}

{#        <label id="hamburger-menu">#}
{#            <nav id="sidebar-menu">#}
            <nav>
                {% include '_components/_navigation.html.twig' %}
                <label class="switch">
                    <input type="checkbox" name="theme_switch">
                    <span class="slider"></span>
                </label>
            </nav>
{#        </label>#}
{#        <div class="overlay"></div>#}
    </div>
</header>
{% block body %}{% endblock %}
</body>
</html>
