<div class="element-center-h">
    {% if outfits is not empty %}
        <h2>My outfits</h2>

        <div style="width: 60vw;">
            <div class="outfit-grid">
                {% for outfit in outfits %}
                    {% include 'outfit/_full.html.twig' %}
                {% endfor %}
            </div>
        </div>
    {% else %}
        <p>No outfit logged for the moment.</p>
    {% endif %}
</div>
